import virtual_node
import logging

class Chord():
    def __init__(self):
        # Set up Logger
        logger = logging.getLogger('chord')
        logger.setLevel(logging.INFO)


if __name__ == "__main__":
    chord = Chord()